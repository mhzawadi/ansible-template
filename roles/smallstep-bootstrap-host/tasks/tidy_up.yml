---
- name: Remove token
  ansible.builtin.file:
    path: /root/token
    state: absent

- name: Remove cert
  ansible.builtin.file:
    path: /etc/ssh/ssh_host_ecdsa_key-cert.pub
    state: absent

- name: Delete step config
  ansible.builtin.file:
    path: /root/.step
    state: absent
